---
layout: article
author: SnailTowardThesun
title: "LeetCode刷题的日子--No.100: 相同的树"
categories: LeetCode
---

相同的树：递归比较左右子树，结构相同且值相等才相同。

题目：

给你两棵二叉树 p 和 q，编写一个函数来检验这两棵树是否相同。
两棵树如果结构相同且对应的节点值相同，则认为它们相同。

示例：

示例 1：
输入：p = [1,2,3], q = [1,2,3]
输出：true
示例 2：
输入：p = [1,2], q = [1,null,2]
输出：false
示例 3：
输入：p = [1,2,1], q = [1,1,2]
输出：false

题目翻译：

给你两棵二叉树 p 和 q，编写一个函数来检验这两棵树是否相同。
两棵树如果结构相同且对应的节点值相同，则认为它们相同。

解题思路：

1. 递归法：
- 如果两个节点都为空，返回 true
- 如果其中一个节点为空，返回 false
- 如果两个节点都不为空，比较节点值
- 递归比较左子树和右子树
2. 算法步骤：
- 检查两个根节点是否为空
- 比较两个根节点的值
- 递归检查左子树
- 递归检查右子树
3. 复杂度分析：
- 时间复杂度: O(n)
- 空间复杂度: O(h)，h 为树的高度

代码：

```cpp
class Solution {
    public:
        bool isSameTree(TreeNode* p, TreeNode* q) {
            if (p == NULL || q == NULL)
                return p == q ? true : false;
            return p->val == q->val
                ? isSameTree(p->left, q->left) && isSameTree(p->right, q->right)
                : false;
        }
};
```
