---
layout: article
author: SnailTowardThesun
title: "LeetCode刷题的日子--No.1722: Minimum Hamming Distance After Swap Operations"
categories: LeetCode
---

Minimum Hamming Distance After Swap Operations：使用并查集构建连通分量，计算最小汉明距离。

汉明距离是信息论中的一个重要概念，由理查德·汉明在1950年提出，用于衡量两个等长字符串之间的差异程度。在计算机科学中，它被广泛应用于错误检测、密码学和DNA序列分析等领域。有趣的是，汉明距离还被用于语音识别和拼写检查系统中，帮助计算机理解人类语言的微小差异。

题目：

https://leetcode.com/problems/minimum-hamming-distance-after-swap-operations/

题目翻译：

给定两个长度相同的整数数组 source 和 target，以及一个数组 allowedSwaps，其中 allowedSwaps[i] = [ai, bi] 表示可以交换数组 source 中索引 ai 和 bi 处的元素任意次数。

返回 source 和 target 之间的最小汉明距离，其中汉明距离是指两个数组对应位置上元素不同的位置数。

示例：

输入：source = [1,2,3,4], target = [2,1,4,5], allowedSwaps = [[0,1],[2,3]]
输出：1

解题思路：

1. **并查集构建连通分量**：使用并查集将可以相互交换的索引合并到同一个连通分量中
2. **统计连通分量内元素频率**：对于每个连通分量，统计 source 中各元素的出现频率
3. **计算最小汉明距离**：遍历 target 数组，检查每个位置的元素是否能在对应连通分量中找到匹配的元素
4. **时间复杂度**：O(nα(n))，其中 α 是阿克曼函数的反函数，近似常数
5. **空间复杂度**：O(n)

代码：

```cpp
class Solution {
public:
    int minimumHammingDistance(vector<int>& source, vector<int>& target, vector<vector<int>>& allowedSwaps) {
        // 并查集：初始化 parent 数组，每个元素的根节点指向自己
        int n = source.size();
        vector<int> parent(n);
        for (int i = 0; i < n; i++) {
            parent[i] = i;
        }

        // 查找函数：带路径压缩的递归查找
        function<int(int)> find = [&](int x) -> int {
            if (parent[x] != x) {
                parent[x] = find(parent[x]);
            }
            return parent[x];
        };

        // 合并函数：将两个元素所在的集合合并
        auto unionSet = [&](int u, int v) {
            int pu = find(u);
            int pv = find(v);
            if (pu != pv) {
                parent[pu] = pv;
            }
        };

        // 根据 allowedSwaps 构建连通分量
        for (auto& swap : allowedSwaps) {
            unionSet(swap[0], swap[1]);
        }

        // 哈希表：记录每个连通分量中各元素的出现频率
        unordered_map<int, unordered_map<int, int>> compFreq;
        for (int i = 0; i < n; i++) {
            int root = find(i);
            compFreq[root][source[i]]++;
        }

        // 计算最小汉明距离：遍历 target，检查每个位置能否在同连通分量内匹配
        int hammingDist = 0;
        for (int i = 0; i < n; i++) {
            int root = find(i);
            auto& freqMap = compFreq[root];
            if (freqMap[target[i]] > 0) {
                freqMap[target[i]]--;
            } else {
                hammingDist++;
            }
        }

        return hammingDist;
    }
};
```